* When adding code, favor a clean design even if that may result in a larger overall code change.
* When writing IF statements, always use curly braces even when the IF statement contains a single instruction.
* A comment that takes up an entire line using either /* or // syntax should be preceded by a blank line. Example:

```
var foo = 5;

//this is a whole line comment
var bar = 6;
```
